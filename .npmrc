# Exact versions in the lockfile and in package.json. A caret range on a
# transitive security fix is how "works on my machine" gets into a rent run.
save-exact=true
# pnpm's default. Stated so a future --shamefully-hoist is a decision, not a fix.
node-linker=isolated
strict-peer-dependencies=true
