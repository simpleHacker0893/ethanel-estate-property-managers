# 11: The nine remaining feature pages

**What to build:** Ten feature pages live, all on one template. Mechanical by construction — if a
page needs something the template cannot express, the template changes rather
than the page growing its own components.

**Blocked by:** 10

**Status:** ready-for-agent

- [ ] Ten feature pages exist in total, all on the single template.
- [ ] Every capability on every page traces to a real service; none invents one (acceptance row 7).
- [ ] Pages describing later-sprint capabilities carry the availability label, including the ones that may never ship.
- [ ] **No page introduces a new component.** If one is genuinely needed, the template changes and every page benefits.
- [ ] The no-invented-proof test still passes across all ten content modules.
